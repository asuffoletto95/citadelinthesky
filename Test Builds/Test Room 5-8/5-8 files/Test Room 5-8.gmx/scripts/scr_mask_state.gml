 ///scr_mask_state

    instance_create(x,y,obj_shield_mask);
  
        if(keyboard_check_released(key_mask_ability))
         {
         image_speed = 1;
         image_index = 0;
         state = scr_move_state;
            }
    










